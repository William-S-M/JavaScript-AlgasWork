var condition = "truue";

// Ao colocar ! antes da variável, ele inverte o valor dela. // !! inverte duas vezes.
// Ou seja, se a variável for true, ela se torna false.

if (condition) {
  console.log("A condição é verdadeira.");
} else {
  console.log("A condição é falsa.");
}
